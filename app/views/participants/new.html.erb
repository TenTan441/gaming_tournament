<% provide(:title, "参加者作成") %>
<h1>参加者追加</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: @participant, url: tournament_participants_path, local: true, method: :post) do |f| %>

      <%= f.label '参加者追加' %>
      <%= select_tag :players, options_from_collection_for_select(User.all, :id, :name), include_blank: true, multiple: true, class: "form-control searchable" %>
      <%= f.submit "参加者追加", class: "btn btn-primary btn-block btn-login" %>
    <% end %>
  </div>
</div>

<script>

</script>